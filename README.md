This is a [Next.js](https://nextjs.org)  bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```


## Deploy on Render - https://smart-tasks-fe.onrender.com/
Note: It is deployed on free instance, so for very first time, it will load slowly (be default nature of render). But later it will work fast.
